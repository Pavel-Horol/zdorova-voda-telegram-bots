# UX.md

Справочник по UX для AI-агента: **как делать бот удобным для клиента**. Это не
план фич и не бизнес-правила (те — в `PRODUCT.md`) и не технические конвенции
(те — в `CLAUDE.md`). Здесь — **принципы, приёмы и антипаттерны** conversational
UX, приземлённые на этот бот доставки воды.

Обоснования, цифры, разбор конкурентов и источники — в `RESEARCH-UX.md` (полный
ресёрч-отчёт). Этот файл — рабочая выжимка «делай так / не делай так»; за «почему
именно так» и данными агент идёт в `RESEARCH-UX.md` по проставленным якорям
(→ RESEARCH §N).

**Как этим пользоваться (для агента).** Перед тем как менять/добавлять текст,
кнопку или шаг сценария — сверься с этим файлом. Правка нарушает принцип из §1 или
попадает в антипаттерн из §4 → не делай так, предложи альтернативу. Формулировки в
§3 — эталоны тона, а не жёсткие требования: держи дух, адаптируй под ситуацию.
Меняешь продуктовое правило заодно — правь `PRODUCT.md` тем же коммитом (`CLAUDE.md`).

**Контекст продукта, который определяет всё остальное.** Бот работает в одном
небольшом городе (Жёлтые Воды). Аудитория — обычные жители, не «продвинутые
пользователи»: пенсионер, занятая мама, офис-менеджер; многие заказывают с телефона
одной рукой. Оплата всегда наличными водителю. Ниша воды живёт на **повторных
заказах** постоянных клиентов, а не на разовой конверсии. Отсюда главный вывод
ресёрча: выигрывает **не «умный», а предсказуемый бот на кнопках** — исследования
(NN/g: боты плохо переносят отклонение пользователя от сценария) и все живые
конкуренты («Сестрица», «202», «Роганська», ЄДВ) сходятся на кнопочном флоу вокруг
«повторить последний заказ». (→ RESEARCH TL;DR, §7)

---

## §1. Принципы (не нарушать)

**П1. Кнопки по умолчанию, свободный ввод — исключение.** Для этой аудитории
свободный текст = источник ошибок и тупиков (люди вводят количество как «две»,
«2шт», «пару»; сумму — пятью разными способами). Свободный ввод оставляем ТОЛЬКО
там, где кнопками не обойтись: адрес и комментарий — и всегда с примером формата.
Количество, тип помпы, да/нет, навигация — всегда кнопки. (→ RESEARCH §1, §3)

**П2. Никаких тупиков (dead ends).** Это ключевой антипаттерн ниши. После любого
ответа бота у клиента есть явный следующий шаг: кнопки, куда идти дальше. Сообщение,
которое ничего не предлагает нажать, — тупик. Ошибка, отмена, «не найдено», «уже
нельзя», непонятый ввод — все заканчиваются понятным «а теперь можно вот это»
(в крайнем случае — напоминанием, что умеет бот). (→ RESEARCH §1, §2)

**П3. Всегда есть выход и восстановление.** На любом шаге ввода клиент может
передумать: «◀ Назад», «❌ Скасувати» или глобальное reply-меню (сбрасывает FSM).
После ошибки — не «начни всё сначала», а «продолжи с места»: введённое сохраняется.
*В этом боте: reply-меню сбрасывает любой FSM, внутри сценария есть inline
«Назад»/«Отмена», «Назад» ходит по стеку истории с сохранением данных. Не ломать.*
(→ RESEARCH §1, §2)

**П4. Подтверждение действия = отражение результата.** Ответ бота подтверждает, что
ввод получен, и показывает результат: «Адресу збережено ✅», «Замовлення прийнято ✅».
Это обратная связь (acknowledgement), без которой пользователь не уверен, что
действие сработало. Молчание после действия — «тихий сбой» в восприятии. Критичные/
необратимые действия подтверждать явно, до выполнения. (→ RESEARCH §2)

**П5. Кнопка говорит, что произойдёт (глагол + объект).** Метка — обещание
результата, а не абстрактная команда. «✅ Усе вірно, замовляю» лучше «ОК»; «💰 Ціни»
понятнее «Інфо». Избегать «submit», «готово», голых «так/ні» в отрыве от контекста:
после «Submit» пользователю неясно, что случится. Для необратимых действий — контекст
прямо в кнопку: не «Підтвердити», а «Підтвердити замовлення». (→ RESEARCH §1, §4)

**П6. Один экран — одна задача, без «простыней».** Не пихать в одно сообщение выбор
количества, адрес и подтверждение (turn-taking: бот спрашивает по одному). Стена
текста на мобильном рассыпается на несколько экранов, и клиент теряет нить. Коротко,
сканируемо; главное (итоговая сумма, следующий шаг) — в конце и заметно. Короткие
формулировки понимаются почти на 100% при длине ≤ ~8 слов. (→ RESEARCH §1, §4)

**П7. Минимум трения на повторе — это ядро продукта.** Повтор заказа — не
второстепенная фича, а главный экран для вернувшегося клиента (весь рынок строится
вокруг него). Путь к повтору: один-два тапа без повторного ввода адреса и количества,
с числом прямо в метке кнопки. Каждый лишний шаг на повторе — прямая потеря оборота.
*В этом боте есть повтор «в один тап» и подставленное прошлое количество — беречь.*
(→ RESEARCH §2, §3, §7)

**П8. Тон — тёплый, человеческий, без жаргона.** Клиент общается с людьми, а не с
автоматом. Никаких «error», «invalid input», кодов, технических деталей. Ошибка —
дружелюбно, без обвинений («Не вдалося розпізнати…», а не «Ви ввели неправильно!»),
с примером и выходом. Эмодзи — как смысловые маркеры (📍 адрес, 💰 деньги, ⚠️
внимание), в меру. (→ RESEARCH §2, §4)

**П9. Предсказуемость и единообразие.** Один объект/действие = одно слово во всём
боте. Отмена — везде «❌ Скасувати», назад — везде «◀ Назад», один язык (украинский,
без русизмов), единый порядок кнопок (подтверждающее — слева/сверху, отмена —
справа/снизу). Разнобой ломает ментальную модель непродвинутого пользователя.
(→ RESEARCH §1, §4)

**П10. Уважать деньги и доверие: цена честно и рано.** Скрытые доп.расходы — причина
№1 брошенных корзин (среди готовых купить — 48%). Всегда до подтверждения: полная
разбивка (вода + тара + …= итог) и явно «готівкою водієві». Никаких доборов
«сюрпризом» и «цену уточнит диспетчер» без числа. Прозрачность = доверие = повтор.
*В этом боте цена замораживается в заказе и показывается на Confirm — это правильно,
см. `PRODUCT.md`.* (→ RESEARCH §1, §3, §10-Caveats)

---

## §2. Приёмы, специфичные для доставки воды

**Повтор заказа — первая кнопка для вернувшегося клиента.** «🔄 Повторити: 2 бутлі»
с числом из прошлого заказа + вторичная «✏️ Змінити». Подставлять адрес и количество
(как «Бот пам'ятає адресу» у ЄДВ). Это дорога по умолчанию, а не пункт в меню.
(→ RESEARCH §3, §7)

**Сохранённый адрес — не спрашивать дважды.** Ручной ввод адреса — одна из главных
точек трения и ошибок. Адрес вводится один раз, дальше подставляется; сменить можно
отдельно, вне заказа. Постоянник никогда не вводит адрес заново. (→ RESEARCH §3, §7)

**Гео-метку ставит диспетчер, не клиент.** Обычному клиенту тяжело и незачем возиться
с координатами. Он вводит адрес словами (минимум трения), точную точку при надобности
ставит диспетчер, который город знает. *В этом боте так — это сильнее среднего по
рынку, не переносить возню с координатами на клиента.* (→ RESEARCH §3, §6, §7)

**Экран подтверждения = «чек до оплаты».** Что везём, куда, почём, итого + «готівкою
водієві». Хорошая микрокопия (готовая, у ЄДВ): подсказка «підготуйте суму без здачі».
Клиент один раз глазами проверяет и жмёт «всё верно» — снимает тревогу и число
ошибочных заказов. (→ RESEARCH §3, §4, §5)

**Тара и залог — объяснять простым языком прямо в потоке.** Рынок делится: одни без
залога (бутыль в собственность), другие с залоговой тарой отдельной позицией. Что бы
ни было в этом боте — в стартовом комплекте одной-двумя фразами показать, что входит
и как работает обмен «порожня → повна» (даёшь пустую — берёшь полную, доплата только
при увеличении числа бутылей). (→ RESEARCH §5)

**Первая развилка онбординга — выгодами.** «🆕 Стартовий комплект: бак + помпа + вода»
одной кнопкой / «💧 У мене вже є баки». Три пункта максимум на экране выбора — больше
= паралич выбора. (→ RESEARCH §5)

**Запрос телефона — с объяснением зачем.** «Щоб водій міг зателефонувати перед
доставкою» рядом с кнопкой шеринга контакта. Объяснение «зачем» снимает тревогу
приватности и повышает заполняемость. «Подзвонити заздалегідь» — опцией через
комментарий, а не обязательным шагом (как у «202»). (→ RESEARCH §3, §4, §5)

**Оценка времени доставки — гасит вопрос №1.** «Коли привезете?» — главный вопрос
после заказа. Пресеты у диспетчера («сьогодні / протягом години / завтра») закрывают
его до звонка. *В этом боте есть — сильный ход.* (→ RESEARCH §3)

**Статусы заказа — умеренно.** 2–3 понятных апдейта максимум (прийнято → передано
водію → в дорозі/сьогодні). Уведомления полезны, но не назойливы; транзакционное не
мешать с промо. (→ RESEARCH §3, §4)

**v2+ (только упоминание, НЕ реализовывать без запроса):** напоминания о воде,
подписки/рекуррентные заказы, реактивация через deep link, онлайн-оплата,
предиктивный реордер, QR на бутыли для реордера. Осознанно вне скоупа (`PRODUCT.md`).
Держать в голове при проектировании модели данных, чтобы потом не переделывать.
(→ RESEARCH Recommendations «На будущее»)

---

## §3. Микрокопирайтинг: эталоны формулировок

Ориентиры тона. Существующие тексты в основном уже соответствуют; отмечено, что
подправить.

### Кнопки действий (глагол + объект, результат ясен)
| Хорошо | Почему |
|---|---|
| `✅ Усе вірно, замовляю` | Говорит результат — создастся заказ. Не «ОК». |
| `🔄 Повторити: 2 бутлі` | Показывает, что именно повторяем (число). |
| `✏️ Змінити адресу` | Глагол+объект, ясно что изменится. |
| `🗑 Прибрати коментар` | Понятно и однозначно. |
| `📱 Поділитися номером` | Точно описывает, чем делимся. |
| `⏭ Пропустити` | Явный опт-аут на необязательном шаге. |

**Антипример:** голое `ОК`, `Готово`, `Так`/`Ні` без контекста, `Надіслати` без
указания чего. Для необратимых/значимых действий контекст — в саму кнопку.

### Подтверждения (отражают результат)
- `Адресу збережено ✅`
- `Замовлення прийнято ✅ Незабаром з вами зв'яжуться / привезуть воду. Дякуємо!`
- `Замовлення скасовано. Можна оформити заново будь-коли.` ← **образец**: даже отмена
  не тупик, а приглашение вернуться.

### Ошибки (проблема простым языком + пример/как исправить + выход)
Шаблон: **[что случилось] + [как исправить/пример] + [кнопка выхода]**, без обвинений
и жаргона. (→ RESEARCH §2, §4)
- `Надішліть, будь ласка, текстом 🙏` (на не-текст вместо адреса) — не сбрасывает экран.
- `Не вдалося оформити замовлення 😔 Спробуйте ще раз або зателефонуйте нам.` —
  признаёт сбой + два выхода.
- `Не вдалося розпізнати адресу. Напишіть вулицю і будинок, напр.: Гагаріна, 12, кв. 5`
  ← **образец с примером формата** (усилить подсказки на шаге адреса).
- `Потрібен саме ваш номер. Натисніть кнопку «Поділитися номером» нижче 🙏`

### Запрос данных (объяснять зачем)
- `Вітаємо! 💧 Щоб оформити замовлення, поділіться номером телефону — за ним ми
  визначимо вашу адресу доставки.` ← **образец**: просит данные + выгода клиенту.

### Что стоит починить в текущих текстах
- **Русизм** (`client-bot.service.ts`): `'Тебя удалено з бази.'` — «Тебя» русское. →
  `'Тебе видалено з бази. /start — почати заново.'` (нарушение П9). *Служебная команда,
  но язык должен быть единым.*
- **`SUPPORT_PHONE` — заглушка** (`(телефон уточнюється)` / placeholder). TODO из
  `PRODUCT.md`: до прода подставить реальный номер, иначе «Зв'язатися» и фолбэки ведут
  в никуда (нарушение П2 — тупик).
- **Единообразие «ти/ви»**: клиентские тексты на «ви» (правильно для аудитории);
  служебные строки удаления сорвались на «ти» (`Спробуй ще раз`). По-хорошему привести
  к «ви» ради П9.

---

## §4. Антипаттерны (так НЕ делать)

Самая ценная часть. Правка попадает сюда — стоп. (Полная таблица «плохо → хорошо» с
источниками — → RESEARCH §1.)

**А1. Тупик после ошибки/отказа/«уже нельзя».** «Це замовлення вже не можна
скасувати» без единой кнопки — dead end, клиент упёрся в стену. → Всегда выход:
reply-меню на месте, кнопка «до головного меню» или подсказка что дальше. На непонятый
ввод — не «Не зрозумів» и тишина, а «Я вмію: замовити воду, повторити, ціни, зв'язок».
*В этом боте reply-меню постоянное — оно и спасает; для новых экранов помнить явно.*

**А2. «Мёртвые» inline-кнопки от прошлых сообщений.** Старое сообщение с кнопками не
погашено → клиент жмёт устаревший экран → действие не к месту или ошибка. Без
`answerCallbackQuery` клиент до минуты видит «часики». → Гасить прошлую клавиатуру при
переходе (в этом боте — `replyInline`/`replyMenu`, прямой `ctx.reply` для экранов
сценария запрещён, см. `CLAUDE.md`); всегда отвечать на callback. UX- и техническое
правило одновременно.

**А3. Свободный ввод там, где хватило бы кнопки.** «Введіть кількість» текстом → «дві»,
«2шт», «пару». → Конечный выбор = кнопки (1–5 + «Інша кількість»). Свободный ввод —
только адрес/комментарий, и с примером формата. (П1)

**А4. Просить то, что бот уже знает.** Заново вводить адрес/количество на повторе,
спрашивать имя, которое есть. → Подставлять известное, давать только менять при
желании. (П7)

**А5. Yes/No и «submit» без контекста.** «Ви впевнені? [Так] [Ні]» — в спешке клиент
не помнит, к чему вопрос. → Контекст в кнопку: `[Так, скасувати замовлення]` /
`[Ні, залишити]`. Особенно для отмен и необратимых действий.

**А6. Технические детали и жаргон наружу.** «Order creation failed», «callback
invalid», «session lost». → Человеческим языком + выход (§3 «Ошибки»). Клиент не
должен видеть, что «внутри».

**А7. Стена текста.** Длинное сообщение, где сумма/действие теряются в середине; на
мобильном рассыпается на экраны. → Коротко, сканируемо, главное — в конце и заметно. (П6)

**А8. Скрытая цена / сюрприз в сумме.** Итог только после создания заказа; добор
залога, о котором не предупредили на Confirm; «цену уточнит диспетчер» без числа. →
Полная разбивка и итог до подтверждения, всегда. (П10; причина №1 брошенных корзин)

**А9. Разнобой в словах и кнопках.** «Отмена»/«Скасувати»/«Стоп» вперемешку; «Назад»
то слева, то справа; смесь языков; «бутель»/«баклажка». → Единый словарь и раскладка. (П9)

**А10. Назойливые уведомления.** Сообщение на каждый чих; промо вперемешку со
статусами. → Уведомлять о значимом (смена статуса, время доставки), транзакционное и
промо разделять. *В этом боте учтено: `CREATED` не уведомляет, отмена клиентом не
дублируется — правильно.*

**А11. Заставлять клиента делать работу системы.** Просить координаты, точный тариф,
номер заказа. → Клиент даёт минимум (адрес словами, количество кнопкой), остальное —
система/диспетчер. (П1, §2 про гео)

**А12. Отмена/повтор как единственный способ исправить.** Ошибся в количестве — не
должен отменять весь заказ. → Точечная правка. *В этом боте на Confirm есть «✏️
Змінити» — правильно, не убирать.*

**А13. Наказывать за возврат назад.** «◀ Назад» сбрасывает весь введённый прогресс. →
Стек истории с сохранением данных (в этом боте — ручной `history: Step[]`, `CLAUDE.md`).

**А14. Иллюзия «свободного разговора» у кнопочного бота (capability ambiguity).** Не
намекать «напишіть, що хочете», если бот понимает только кнопки — иначе клиент пишет
свободно и упирается в непонимание. → Честно вести кнопками; свободный ввод только
там, где он реально обрабатывается. (→ RESEARCH §1)

---

## §5. Чеклист перед изменением UX (для агента)

Любой «нет» — пересмотри.

- [ ] Есть явный следующий шаг после каждого нового сообщения? (нет тупиков — А1, П2)
- [ ] Гасится прошлая inline-клавиатура при переходе, отвечаем на callback? (А2)
- [ ] Конечный выбор — кнопками, не свободным вводом? (П1, А3)
- [ ] Свободный ввод (адрес/коммент) — с примером формата? (П1, §3)
- [ ] Метка новой кнопки говорит, что произойдёт (глагол+объект)? (П5, А5)
- [ ] Показан результат/подтверждение после действия клиента? (П4)
- [ ] Для необратимого действия — контекст в кнопке? (А5)
- [ ] Ошибки — по-человечески, с примером и выходом, без жаргона? (П8, А6)
- [ ] Не просим то, что уже знаем? Повтор — быстрый (1–2 тапа)? (П7, А4)
- [ ] Цена и «готівкою водієві» видны до подтверждения? (П10, А8)
- [ ] Слова, эмодзи-маркеры, раскладка кнопок, язык — единообразны? (П9, А9)
- [ ] Не плодим лишние уведомления, промо отдельно от статусов? (А10)
- [ ] Не создаём иллюзию свободного диалога у кнопочного бота? (А14)
- [ ] Правило продукта поменялось → обновлён `PRODUCT.md` тем же коммитом?

---

## §6. Что в этом боте уже сделано хорошо (не сломать при рефакторинге)

Чтобы агент случайно не «улучшил» правильное. По ресёрчу — это сильнее среднего по
рынку (→ RESEARCH §7 «Вывод по конкурентам»).

- **Повтор заказа в один тап** с числом в метке — ядро retention (§2, П7).
- **Постоянное reply-меню**, отменяющее любой FSM — надёжный выход отовсюду (П3, А1).
- **Структурированный Confirm** с разбивкой цены и «готівкою водієві» — прозрачность и
  доверие (П10). Заморозка `totalPrice` — корректная история.
- **Гео-метку ставит диспетчер, адрес клиент вводит словами** — трение на клиенте
  минимально (§2).
- **Пресеты времени доставки** у диспетчера — гасят «коли?» до звонка (§2).
- **Точечная правка заказа** («✏️ Змінити») вместо «отмени и начни заново» (А12).
- **«Назад» через стек истории** с сохранением введённого (А13).
- **Дисциплина гашения клавиатур** (`replyInline`/`replyMenu`) — нет мёртвых кнопок (А2).
- **Умеренные уведомления** (CREATED молчит, отмена не дублируется) (А10).
- **Пресеты у диспетчера** вместо свободного ввода — меньше ошибок оператора (→ RESEARCH §1).
- **Тёплый тон и осмысленные эмодзи-маркеры** в клиентских текстах (П8).

---

## §7. Telegram-специфика (техника ↔ UX)

Кратко; детали и лимиты — → RESEARCH §6.

- **Reply vs Inline.** Reply-клавиатура = постоянная навигация/главное меню (живёт под
  полем ввода, палец-френдли, `is_persistent`). Inline = шаги внутри сценария и действия
  по конкретному сообщению. Reply-кнопка отправляет ровно свой текст (нельзя показать
  одно, отправить другое — для этого inline). *В этом боте разделение по ролям — верно.*
- **Гасить висящие клавиатуры.** Редактировать сообщение (`editMessageText`/
  `editMessageReplyMarkup`), а не слать новое; всегда `answerCallbackQuery`. (А2)
- **callback_data ≤ 64 байт.** Короткие идентификаторы (`ord:rep`, `qty:3`), состояние —
  на сервере/в БД по ключу. Кириллица/эмодзи «съедают» байты быстрее; превышение →
  ошибка. *В этом боте callback'ы уже короткие — держать.*
- **Лимиты кнопок.** До 8 в ряд (практично 3–4 для мобильного); длинные списки —
  пагинация, не «простыня». Текст кнопки короткий (~15 симв., иначе перенос).
- **requestContact/requestLocation** — только в личке, с подтверждением клиента. Гео у
  диспетчера — оставить так (клиенту проще).
- **input_field_placeholder** — подсказка в поле ввода помогает («Напишіть вулицю і
  будинок»).
- **Deep linking** (`t.me/bot?start=…`) — задел под v2 (реактивация, приглашение
  постоянников), сейчас вне скоупа.

---

## Границы применимости (важно для агента)
- Цифры по брошенным корзинам (Baymard, Dynamic Yield) — из веб-чекаутов, не из
  Telegram-ботов. **Тренды** переносимы (трение, скрытые цены, лишние поля), **абсолютные
  числа — нет**, особенно для бота с оплатой наличными. Не цитировать цифры клиенту как
  «доказано для нашего бота». (→ RESEARCH Caveats)
- Оплата наличными водителю — не недостаток, а снятие крупнейшего барьера чекаута
  (регистрация карты и security-опасения). Но обязательно писать «готівкою водієві» на
  Confirm (управление ожиданиями). (→ RESEARCH §5)
- Всё про напоминания/подписки/онлайн-оплату/предиктивный реордер — **v2+**, здесь
  только как «на будущее», не задача к реализации. (`PRODUCT.md`)
- «Умные»/генеративные диалоги, свободный NLU — **сознательно не применяем**: для этой
  аудитории и задачи предсказуемый кнопочный бот надёжнее (NN/g: боты плохо переносят
  отклонение от сценария). (→ RESEARCH Caveats)

---
*Полное обоснование, данные и разбор конкурентов — в `RESEARCH-UX.md`.*
