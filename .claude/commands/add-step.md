Добавь новый шаг в FSM клиентского бота: $ARGUMENTS

FSM здесь ручной (НЕ scenes, НЕ conversations plugin) — состояние в in-memory
session grammY. Чтобы шаг не сломался, тронь ВСЕ перечисленные точки. Пропуск
любой = тихий баг (мёртвая кнопка, потерянный «Назад», провал гарда).

Перед началом прочитай `CLAUDE.md` (секция «Как устроен FSM») и `PRODUCT.md`
(поведение клиентского бота), чтобы понять, куда шаг встраивается в сценарий.

Точки, которые надо тронуть в `src/bots/client-bot/client-bot.service.ts`:

1. **enum `Step`** — добавь новое состояние.
2. **`ORDER_FLOW_STEPS`** — добавь шаг сюда, ЕСЛИ он внутри сценария заказа
   (на нём должны работать reply-кнопки-выход «Заказ отменён» и inline «Назад»/«Отмена»).
3. **`SessionData`** — если шаг хранит данные между переходами, добавь поле
   (по образцу `bottles?`, `addressRaw?`). Снабди комментарием: когда живёт, когда чистится.
4. **render-хелпер** (по образцу `renderChooseQty`/`renderConfirm`): ставит
   `ctx.session.step = Step.X` и отправляет экран ТОЛЬКО через `replyInline`
   (inline-сценарий) или `replyMenu` (reply-меню). НИКОГДА не `ctx.reply` напрямую.
5. **Гард** в хендлере, который обрабатывает действие на этом шаге:
   `if (ctx.session.step !== Step.X) return;` — первой строкой после answerCallbackQuery.
6. **`pushHistory(ctx, Step.X)`** — на входе в шаг, ЕСЛИ это inline-переход внутри
   флоу (чтобы «Назад» вернул сюда).
7. **`onBack`** — добавь ветку возврата НА этот шаг (если на него можно вернуться).
8. **`resetSession`** — если в п.3 добавил поле сессии, очисти его здесь.
9. **callback-идентификаторы + регистрация** в `registerHandlers` — если шаг вводит
   новые кнопки (по образцу `CB_*` констант и `bot.callbackQuery(...)`).
10. **Тексты** — добавь в `client-bot.texts.ts`, не хардкодь строки в сервисе.

После реализации:
- если поведение влияет на продукт — обнови `PRODUCT.md` тем же изменением;
- `npm run lint && npm test`;
- кратко перечисли, какие из 10 точек ты тронул и почему остальные не понадобились.

НЕ добавляй scenes/conversations plugin. НЕ заменяй ручное гашение клавиатур.
